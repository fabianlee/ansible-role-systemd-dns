---

- name: include systemd-dns tasks
  include_task: systemd-dns.yml
  when: ansible_distribution_major_version|int != 16

- name: include resolveconf tasks
  include_task: resolveconf.yml
  when: ansible_distribution_major_version|int == 16


